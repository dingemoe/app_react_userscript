{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "git sync saves",
      "type": "shell",
      "command": "git add -A && git diff --cached --quiet && exit 0; git commit -m \"sync saves\"; git push",
      "problemMatcher": [],
      "group": "none"
    }
  ]
}
